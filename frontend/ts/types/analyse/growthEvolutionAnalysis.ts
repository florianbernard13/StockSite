export interface GrowthEvolutionAnalysis {
    amplitude: number;
    deltas: number[];
    deltas_pct: number[];
    total_delta_pct: number;
    instability: number;
    volatility: number;
    volatility_pct: number;
}